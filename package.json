{
  "name": "shared-wealth-international-deploy",
  "version": "1.0.0",
  "description": "Shared Wealth International Platform",
  "scripts": {
    "build": "cd wealth-pioneers-network && pnpm install && vite build && tsc -p tsconfig.server.json",
    "build:prod": "cd wealth-pioneers-network && pnpm install && vite build && tsc -p tsconfig.server.json",
    "server:build": "cd wealth-pioneers-network && tsc -p tsconfig.server.json",
    "server:start": "cd wealth-pioneers-network && node dist/server/server.js",
    "start": "cd wealth-pioneers-network && node dist/server/server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/msalmanmunirmalik/Shared-Wealth-International.git"
  },
  "author": "Salman Malik",
  "license": "MIT",
  "devDependencies": {
    "puppeteer": "^24.22.3"
  }
}
